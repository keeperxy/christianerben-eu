interface:
  display_name: "Christian Erben Update"
  short_description: "Check Bun deps, then run visual regression checks if needed"
  default_prompt: "Use $christianerben-update to check for outdated Bun dependencies first, and only if updates exist create an update branch, bump this portfolio to the latest Bun package versions, compare before-and-after screenshots, fix regressions, and open a PR against development."
